// Seperate text from image
const f5keys = document.getElementById('');
const imageFiles = document.getElementById('');
const showCiphertext = document.getElementById('');
const seperateImageButton = document.getElementById('');

// Decrypt the ciphertext
const ciphertext = document.getElementById('');
const privateKey = document.getElementById('');
const decrypt = document.getElementById('');


